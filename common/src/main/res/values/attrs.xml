<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 流布局-->
    <declare-styleable name="FlowLayout">
        <!--支持折叠 -->
        <attr name="flow_fold" format="boolean" />
        <!--折叠时的行数（支持折叠情况下） -->
        <attr name="flow_foldLines" format="integer" />
       <!--折叠时的最大行数（支持折叠情况下） -->
        <attr name="flow_fold_max_lines" format="integer" />
        <!--平均分配 -->
        <attr name="flow_equally" format="boolean" />
        <!--平局分配一行的数量 -->
        <attr name="flow_equally_count" format="integer" />
        <!--水平间距 -->
        <attr name="flow_horizontalSpacing" format="dimension" />
        <!--竖直间距 -->
        <attr name="flow_verticalSpacing" format="dimension" />
        <!--左右对齐 -->
        <attr name="flow_gravity">
            <enum name="LEFT" value="0" />
            <enum name="RIGHT" value="1" />
        </attr>
    </declare-styleable>
    <declare-styleable name="CircleTextImageView">
        <attr name="borderWidth" format="dimension" />
        <attr name="borderColor" format="color" />
        <attr name="borderOverlay" format="boolean" />
        <attr name="android:textSize" />
        <attr name="android:textColor" />
        <attr name="android:text" />
        <attr name="backgroundColor" format="color" />
        <attr name="textPadding" format="dimension" />
    </declare-styleable>
    <!-- 仪表盘自定义属性 -->
    <declare-styleable name="MySeekBar">
        <!--背景颜色-->
        <attr name="bgColor" format="color"/>
        <!--进度颜色-->
        <attr name="progressColor" format="color"/>
        <!--当前进度-->
        <attr name="progress" format="float"/>
        <!--总进度-->
        <attr name="maxProgress" format="float"/>
        <!--是否显示小太阳-->
        <attr name="showSun" format="boolean"/>
        <!--是否缩放小太阳-->
        <attr name="zoomSun" format="boolean"/>
        <!--小太阳颜色-->
        <attr name="sunColor" format="color"/>
        <!--小太阳半径-->
        <attr name="circleRadius" format="dimension"/>
        <!--矩形圆角-->
        <attr name="radiusXY" format="dimension"/>
        <attr name="showText" format="boolean"/>
        <attr name="setTextColor" format="color"/>
        <attr name="setTextHeight" format="integer|dimension"/>
        <attr name="setTextSize" format="dimension"/>
    </declare-styleable>

    <declare-styleable name="MarqueeTextView2">
        <attr name="android:textSize" />
        <attr name="android:textColor" />
        <attr name="android:text" />
        <!--循环模式-->
        <attr name="marqueeRepeat" format="enum">
            <!-- 播放一次 -->
            <enum name="single" value="1" />
            <!-- 单项循环 -->
            <enum name="singleLoop" value="0" />
            <!-- 填充后循环 -->
            <enum name="fillLoop" value="-1" />
        </attr>
        <!--每个item之间的距离-->
        <attr name="marqueeItemDistance" format="dimension" />
        <!--开始的起始位置 按距离控件左边的百分比 0~1之间 -->
        <attr name="marqueeStartLocationDistance" format="float" />
        <!--播放速度 也就是文字滚动速度-->
        <attr name="marqueeSpeed" format="float" />
        <!--重新改变内容的时候 ， 是否初始化 位置，默认为true，改变-->
        <attr name="marqueeResetLocation" format="boolean" />
    </declare-styleable>

    <declare-styleable name="MarqueeTextView">
        <!--尺寸是否自适应， 默认false-->
        <attr format="boolean" name="mtv_isAutoFit"/>
        <!--控件是否自动显示与隐藏（根据显示数量）， 默认false-->
        <attr format="boolean" name="mtv_isAutoDisplay"/>
    </declare-styleable>
</resources>